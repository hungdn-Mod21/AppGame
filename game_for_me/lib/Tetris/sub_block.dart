import 'package:flutter/material.dart';

class SubBlock {
  int x;
  int y;
  Color color;

  SubBlock(this.x, this.y, [this.color = Colors.transparent]) {
    this.x = x;
    this.y = y;
    this.color = color;
  }

  // SubBlock(int x,int y, [Color color = Colors.transparent]){

  // }
}
